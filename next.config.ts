const nextConfig = {
  experimental: {
    serverActions: {
      allowedOrigins: [
        "localhost:3000",
        "z0xqsss1-3000.inc1.devtunnels.ms",
      ],
    },
  },
};

export default nextConfig;